{
  "name": "homework-v34-2025",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "dev:backend": "cd backend && npm install && ts-node index.ts",
    "build:frontend": "cd frontend && npm run build",
    "start:backend": "cd backend && node dist/index.js",
    "test": "echo \"Running tests...\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/express": "^4.17.21",
    "ts-node": "^10.9.1"
  }
}
